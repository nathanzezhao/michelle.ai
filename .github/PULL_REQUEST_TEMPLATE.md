## Summary

<!-- What changed and why (1–3 sentences) -->

## Test plan

- [ ] `pytest` passes locally (`LLM_PROVIDER=mock`, `INTENT_MODE=rules`)
- [ ] Manual check in Electron if UI changed (`f` after `index.html` / `main.js` edits)
- [ ] No secrets or `michelle.db` in the diff

## Screenshots / notes

<!-- Optional: orb UI, composer, action confirm flow -->
